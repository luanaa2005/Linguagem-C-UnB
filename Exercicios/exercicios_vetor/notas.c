//O professor Arquimedes precisa da sua ajuda para descobrir qual é a nota mais frequente entre
//as notas. Se houver mais de uma nota mais frequente, você deve imprimir a maior delas! 

#include <stdio.h>

int main(){

    int n, j;
    scanf("%d", &n);

    int v[n], v[j];

    for(int i=0; i<n; i++){
        scanf("%d", &v[n]);
    }

    //quantidade de vezes que um elemento aparece no vetor:
    for(int i=0; i<0; i++){
        if(v[i])

    }
    


    return 0;
}